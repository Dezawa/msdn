<h3><%= @TYTLE %> </h3>
<table border=1><%= label_line_option %>
<% id = @model.id -%>
<tr id='pro_<%= id %>' ><% @labels.each{|html_cell| -%>
  <%= raw html_cell.td -%><%= html_cell.disp(@model) -%></td>
 <%- } -%>
</tr>
</table>

<p>
<table><tr><td rowspan=4>
  <table border=1 ><tr><td colspan=4>
  <%= add_edit_buttoms(:assosiation,:add_action => :add_assosiation,
                     :edit_action => :edit_assosiation,
                     :id => @model.id) %></td></tr>
  <%= label_line_comm 2,@AssosiationLabels %>
  <% @assosiations.each do |obj|  -%>
    <tr id='<%= obj.id %>'><% @AssosiationLabels.each{|html_cell| 
        next if html_cell.class==HtmlHidden || html_cell.class==HtmlPasswd ||  html_cell.field_disable(controller) -%>
  <%= html_cell.td %><%= html_cell.disp_field obj %></td><% } %>
  <%= delete_connection_if_accepted(obj) %>
  </tr>
  <% end %>
  </table></td>
  <tr><td>
    <table border=1>
  <td arrign=center colspan=<%= @WallLabels.size%>>壁</td></tr>
    <%= label_line_comm 2,@WallLabels %>
    <% @model.walls.each do |obj|  -%>
    <tr id='<%= obj.id %>'><% @WallLabels.each{|html_cell| 
        next if html_cell.class==HtmlHidden || html_cell.class==HtmlPasswd ||  html_cell.field_disable(controller) -%>
  <%= html_cell.td %><%= html_cell.disp_field obj %></td><% } %>
  </tr>
  <% end %>
    </table>
  </td></tr>
  <tr></tr>
   <tr><td>
    <table border=1>
   <tr><td colspan=<%= @PillarLabels.size%> >柱</td></tr>
    <%= label_line_comm 2,@PillarLabels %>
    <% @model.pillars.each do |obj|  -%>
    <tr id='<%= obj.id %>'><% @PillarLabels.each{|html_cell| 
        next if html_cell.class==HtmlHidden || html_cell.class==HtmlPasswd ||  html_cell.field_disable(controller) -%>
  <%= html_cell.td %><%= html_cell.disp_field obj %></td><% } %>
  </tr>
  <% end %>
    </table>
</td></tr>
</table>
<br />
<hr>
<%= raw @PostMessage %>
